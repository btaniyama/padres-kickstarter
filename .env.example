# Upstash Redis (https://console.upstash.com/) — required for a shared global pledge total in production.
# Leave unset for local dev: totals use in-memory storage (resets when the dev server restarts).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
